name: Validate NAntUtilities

on:
  push:
    branches:
      - master

jobs:
  Validate:
    runs-on: windows-latest

    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v2

      - name: Package
        id: package
        run: |
          cd NAntUtilities
          dotnet pack